{
  "name": "mcp-toolbox",
  "version": "2.0.0",
  "description": "Serveur MCP utilitaire sur Cloudflare Workers (base64, hash, dates, regex, etc.)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
